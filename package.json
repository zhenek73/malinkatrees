{
    "name": "malinkatrees",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "CI=false cd frontend && npm install && npm run build && cd ../backend && npm install",
      "start": "cd backend && npm start"
    },
    "devDependencies": {
      "typescript": "^5.6.0"
    },
    "description": "Telegram Mini App для новогодней ёлки с токенами MALINKA",
    "repository": {
      "type": "git",
      "url": "https://github.com/zhenek73/malinkatrees"
    }
  }